# Add your own tasks in files placed in validators/tasks ending in .rake,
# for example validators/tasks/capistrano.rake, and they will automatically be available to Rake.

require_relative 'config/application'

Rails.application.load_tasks
